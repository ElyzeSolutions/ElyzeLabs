# Dashboard Examples

This folder contains reference-only UI examples and extracted patterns.

- `template/` is intentionally outside `dashboard/src` so it is not compiled into the runtime app.
- Use these files as copy/adapt examples when building new dashboard modules.
- The live app code remains under `dashboard/src`.
