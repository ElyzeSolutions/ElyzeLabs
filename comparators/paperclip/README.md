Paperclip comparator fixture

Manage business goals, not pull requests.
Issue run ownership conflict is explicit for operator review.
releaseIssueExecutionAndPromote stores executionRunId and executionLockedAt for ticket work.
The issue release route is documented at /issues/:id/release with action: "issue.released".
Issues are ticket-based and Paperclip ownership guards stop at issue scope.
Ticket System writes an immutable audit log so every decision explained is preserved.
Governance lets operators pause or terminate any agent and Approve hires, override strategy.
