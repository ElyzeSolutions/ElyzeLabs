Symphony comparator fixture

Symphony monitors a Linear board and relies on tracker.kind plus LINEAR_API_KEY.
Workers can surface PR review feedback after work is ready.
Isolated workspaces record workspace.root before agent execution.
The release_issue_claim path records claimed work for retries.
reconcile_running_issue_states keeps tracker assignments aligned with active workers.
release claim ownership stays bound to Linear tracker state.
StatusDashboard exposes operator state for live runs.
workspace.before_remove runs cleanup hooks before removing issue workspaces.
This low-key engineering preview is intended for trusted environments.
